<%- include('../partials/head') %>
    <div class="container mx-auto">
        <header class=" flex items-center justify-between my-8">
            <a class="text-2xl font-bold underline bg-purple-400 cursor-pointer">brand!</a>
            <ul class="flex items-center ">
                <% routes.forEach((route)=>{ %>
                    <li class="mx-4 rounded bg-gray-200"> <a href="<%= route.path %>">
                            <%=route.name%>
                        </a>
                        <% }) %>

            </ul>
        </header>
    </div>